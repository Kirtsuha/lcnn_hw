defaults:
  - model: lcnn
  - metrics: antispoof_inference
  - datasets: asvspoof_eval
  - dataloader: asvspoof
  - transforms: none
  - _self_

data_dir: ???
protocol_dir: ???
inferencer:
  device_tensors: [data_object, labels]
  device: auto
  save_path: submission
  seed: 1
  from_pretrained: ???
